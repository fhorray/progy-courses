#[cfg(test)]
mod tests {
    #[test]
    fn test_mutability() {
        // This test just checks if it compiles, which the runner handles.
        // But let's add a dummy test.
        assert_eq!(2 + 2, 4);
    }
}
